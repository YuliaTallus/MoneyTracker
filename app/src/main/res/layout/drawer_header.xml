<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height= "@dimen/drawer_header_height"
    android:background="@mipmap/drawer_patt"
    android:orientation = "vertical"
    android:paddingTop="@dimen/drawer_header_padding_top"
    android:paddingLeft="@dimen/drawer_header_padding_left"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">


    <ImageView
        android:layout_width="@dimen/picture_size"
        android:layout_height="@dimen/picture_size"
        android:src="@mipmap/av"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/linear_height"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/name"
            android:textSize="@dimen/text_size"
            android:fontFamily="sans-serif-medium"
            android:textStyle="bold"
            />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/email"
            android:textSize="@dimen/text_size"
            android:fontFamily="sans-serif"
            android:textColor="@color/text_color"/>

        </LinearLayout>

</RelativeLayout>